<% content_for(:title, @post.date) %>

<%= tag.div class: 'mb-3' do %>
<%= link_to "Back to #{@post.post_type.name} Posts", post_type_path(@post.post_type), class: button_class('btn btn-primary mt-3') %>
<% end %>

<%= render @post %>
