<footer class='footer--delete'>
  <%= link_to "Delete #{object.class.name}", 
      object, 
      data: { 
        turbo_method: :delete, 
        turbo_confirm: "Are you sure you want to delete this #{object.class.name}? This action cannot be undone."
      }, 
      class: 'btn btn-sm btn-outline-danger mb-5' %>
</footer>
